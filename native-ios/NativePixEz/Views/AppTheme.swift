import SwiftUI
import UIKit

enum AppTheme {
    static let background = Color(UIColor.systemBackground)
    static let elevated = Color(UIColor.secondarySystemGroupedBackground)
    static let surface = Color(UIColor.tertiarySystemFill)
    static let subtleFill = Color(UIColor.quaternarySystemFill)
    static let separator = Color(UIColor.separator)
    static let primaryText = Color(UIColor.label)
    static let secondaryText = Color(UIColor.secondaryLabel)
    static let tertiaryText = Color(UIColor.tertiaryLabel)
    static let accent = Color(red: 0.37, green: 0.62, blue: 0.98)
}